# Core data stack
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
pyarrow==17.0.0        # parquet engine

# Data sourcing
# NB: yfinance n'est PAS épinglé dur. Il casse à chaque changement du backend
# Yahoo ; figer une version expose à des erreurs "Expecting value"/YFTzMissing.
# On garde un plancher et on suit les releases.
yfinance>=1.4.1
curl_cffi>=0.7.0   # impersonation requise par yfinance pour éviter le blocage Yahoo

# Forecasting layer
scikit-learn==1.5.2
torch==2.4.1           # LSTM / Transformer
statsmodels==0.14.4    # ARIMA baseline (annexe)

# Optimization layer
cvxpy==1.5.3

# Visualisation / reporting
matplotlib==3.9.2
seaborn==0.13.2

# Tooling
tqdm==4.66.5
pytest==8.3.3
